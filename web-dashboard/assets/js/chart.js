// Placeholder for Chart.js. To display charts on the dashboard you
// would include the Chart.js library here. This stub defines a
// minimal constructor so code that instantiates new Chart() does not
// throw an error if the real library is not present.

class Chart {
    constructor(ctx, config) {
        console.warn('Chart.js stub loaded. No charts will be rendered.');
    }
}

export { Chart };